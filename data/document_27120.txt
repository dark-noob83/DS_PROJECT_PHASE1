If you just want to obtain a comma-separated string, you may use this shortcut: Or this one, if your objects contain square brackets: Finally, you may use map() to convert each item in the list to a string, and then join them: 1  Documentation. 2  Python.
1. [item for sublist in l for item in sublist]. This is a duplicate of a number of other questions. See, for example, stackoverflow.com/q/952914/623518, stackoverflow.com/q/406121/623518 and stackoverflow.com/q/457215/623518. – Chris Aug 8 '12 at 8:25. @zrxq It isn't that question but it is a duplicate of the ones mentioned by Chris – jamylak Aug 8 '12 at 8:27.
There are a few useful tips to convert a Python list (or any other iterable such as a tuple) to a string for display. First, if it is a list of strings, you may simply use join this way: Using the same method, you might also do this:
Learn How To Unlist (remove) your phone number from True Caller. Please Subscribe Here-https://www.youtube.com/channel/UC19W...
Possible Duplicate: Making a flat list out of list of lists in Python. Possible Duplicate: Making a flat list out of list of lists in Python. I am trying to find an easy way to to turn a multi dimensional (nested) python list into a single list, that contains all the elements of the sublists.
itertools.chain(*iterables): Make an iterator that returns elements from the first iterable until it is exhausted, then proceeds to the next iterable, until all of the iterables are exhausted. Used for treating consecutive sequences as a single sequence. Use itertools.chain :